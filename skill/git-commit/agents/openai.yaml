interface:
  display_name: "Git Commit"
  short_description: "Inspect the full working tree, propose commit groups with body suggestions, review candidates, and prepare each local commit"
  default_prompt: "Use $git-commit to inspect the full working tree, propose commit groups with body suggestions, review candidates, suggest verification commands, and prepare each local commit with explicit confirmation."
